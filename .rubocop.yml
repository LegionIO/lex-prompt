inherit_gem:
  rubocop-legion: config/lex.yml

# Runner is used as a mixin — extend self pattern not applicable
Legion/Extension/RunnerIncludeHelpers:
  Enabled: false

# Private helper methods return scalars — not public runner return values
Legion/Extension/RunnerReturnHash:
  Enabled: false
